{
  "name": "tailwind-boilerplate-vanilla",
  "version": "1.0.0",
  "description": "Boilerplate for tailwind with no frameworks. Change file path in content if there are multiple html files being added or the file structure is being changed.",
  "main": "index.js",
  "scripts": {
    "twbuild": "tailwindcss -i ./input.css -o ./css/style.css",
    "twwatch": "tailwindcss -i ./input.css -o ./css/style.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.1.0",
    "prettier-plugin-tailwindcss": "^0.5.9",
    "tailwindcss": "^3.3.6"
  }
}
